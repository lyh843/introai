# 强化学习

## 一、强化学习简介

### 搜索：

只能在确认环境中进行，得到的是从起点到终点动作序列

### 强化学习：

**非确定环境**，学会一个策略，能够在**任意状态**下找到最佳动作。

### 序列决策任务：

一个序列任务，通过各种动作达到自己目标。

智能体从交互中学习得到最大化累计奖励的行动。

### 关键要素

1. 状态（state）：$s \in S$ 对当前环境的描述。

2. 动作 （action) ：$a \in A$ 对智能体采取的行动

3. 策略（policy）：$\pi$ 如何根据观察到的状态做决策，从动作空间中选择一个动作。

   > 强化学习最终学得的就是一个策略函数。

   - 确定性策略：$\pi: S \rightarrow A$
   - 随机性策略：$\pi: S \times A \rightarrow \R$

4. 状态转移（state transition）：$P: S \times A \times S \rightarrow \mathbb{R}$ 从当前t时刻状态$s_t$转移到下一个时刻的状态$s_{t+1}$。

5. 奖励（reward）：$R: S \times A \times S \rightarrow \mathbb{R}$ 执行一个动作后，环境返回给智能体一个数值。

   > 奖励函数的好坏影响强化学习的结果。

6. 轨迹（trajectory）： 智能体观测到的所有状态、动作、奖励的序列。

   > $s_1, a_1, r_1, s_2, a_2, r_2, \cdots, s_n,a_n,r_n$

7. 回报（return）：从当前时刻开始到本回合结束的所有奖励总和。

   > $G_t = R_t + R_{t+1}+\cdots$

   **强化学习的目标是最大化回报，即累计奖励，而不是最大化当前奖励。**

   通常考虑折扣回报，即当前时刻的奖励具有更大权重。

   > $\gamma \in [0, 1]$是未来奖励的折扣因子，使得和未来奖励相比起来智能体更重视即时奖励
   >
   > $G_t = R_t + \gamma R_{t+1} + \gamma^2 R_{t+2}+\cdots$

8. 回合（episode）：一局游戏。

### 交互过程

- 观察到状态$s_t$，根据策略$\pi(s_t)$ 选择动作 $a_t$
- 执行动作$a_t$，状态转移至$s_{t+1}$，获得奖励$r_t$

## 二、马尔科夫决策过程(Markov Decision Process, MDP)

### 1. 基本概念

- **状态变量**：给定时刻$t$，随机变量$X$的取值$X_1, \cdots, X_t$

- **初始状态概率**：随机变量$X$的先验概率分布
- **转移模型**(transition model)：指定世界如何随时间演变，给定过去的状态变量取值之后，确定最新状态的概率分布$P(X_t|X_1X_2,\dots X_{t-1})$

==**马尔可夫假设**：当前状态只依赖于**有限**的**固定数量**的过去状态。==

**状态 + 状态转移 = 马尔可夫过程**

### 2. 定义

具有**马尔可夫性质**的随机过程。

状态$S_t$是马尔可夫的，当且仅当：
$$
\mathbb{P}[S_{t+1}|S_t] = \mathbb{P}[S_{t+1}|S_1,\dots,S_t]
$$

> 强化学习对应了马尔可夫四元组：$(S, A, P, R)$
>
> **有模型学习**(model-based learning)：假设$S, A, P, R$均已知。

**MDP模型的动态转换**：

- 从状态$s_0$开始，智能体选择某个动作$a_0 \in A$，智能体得到奖励$r_0$

- MDP随机转移到下一个状态$s_1 \sim P_{s_0a_0}$

- 这个过程不断进行
  $$
  s_0 \xrightarrow{a_0,r_0} s_1 \xrightarrow{a_1, r_1} s_2 \xrightarrow{a_2, r_2} s_3
  $$

- 智能体的总回报为：$r_0 + \gamma r_1 + \gamma^2 r_2 + \cdots$

### 3. 一个例子——Grid World

> 环境：有若干网络单元组成的网格世界
>
> 可通行单元 / 禁止进入单元 / 目标单元 / 边界
>
> 任务：给定任意起始位置，寻找一条通往目标的"较好"路径。（如何定义“较好”？）

- 状态：在grid world中，状态就是agent所在的位置。
- 状态空间：所有状态的集合$\{S_1, S_2, \cdots, S_9\}$(假设一共只有9个格子)。
- 动作：上下左右、不动
- 动作空间：所有**可行动作**的集合
- 状态转移概率：$P(S_2|S_1, a_2) = \pi(a_2 | S_1)$自定。
- 奖励函数：
  - 越出边界：$r_{bound} = -1$
  - 进入禁止单元：$r_{forbid} = -1$
  - 到达目标单元：$r_{target} = 1$
  - 其他情况，奖励$r=0$

### 4. 有模型学习



